.page-header
  Inköpare

%p
  %strong= model_class.human_attribute_name(:first_name) + ':'
  %br
  = @buyer.first_name
%p
  %strong= model_class.human_attribute_name(:last_name) + ':'
  %br
  = @buyer.last_name
%p
  %strong= model_class.human_attribute_name(:phone) + ':'
  %br
  = @buyer.phone
%p
  %strong= model_class.human_attribute_name(:email) + ':'
  %br
  = @buyer.email
%p
  %strong= model_class.human_attribute_name(:org_id) + ':'
  %br
  = @buyer.org_id

.form-actions
  = link_to t('.back', :default => t("helpers.links.back")), buyers_path, :class => 'btn'
  = link_to t('.edit', :default => t("helpers.links.edit")), edit_buyer_path(@buyer), :class => 'btn btn-primary'
  = link_to t('.destroy', :default => t("helpers.links.destroy")), buyer_path(@buyer), :method => "delete", :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-danger'
