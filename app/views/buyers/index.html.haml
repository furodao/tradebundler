%h1 Listing buyers

%table
  %thead
    %tr
      %th First name
      %th Last name
      %th Org
      %th
      %th
      %th

  %tbody
    - @buyers.each do |buyer|
      %tr
        %td= buyer.first_name
        %td= buyer.last_name
        %td= buyer.org_id
        %td= link_to 'Show', buyer
        %td= link_to 'Edit', edit_buyer_path(buyer)
        %td= link_to 'Destroy', buyer, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Buyer', new_buyer_path
